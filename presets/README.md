# Presets

This directory is reserved for `ironclaw-zh` deployment and model presets.

Planned areas:

- local deployment presets
- Docker deployment presets
- Chinese-friendly model provider presets
